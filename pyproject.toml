[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sample-similarity-explorer"
version = "0.2.0"
description = "Sample-level similarity analysis for high-dimensional biological data"
authors = [{ name = "Your Name" }]
dependencies = ["numpy", "pandas", "seaborn", "matplotlib", "scipy"]

